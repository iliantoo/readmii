# Configuration PostgreSQL
# Format: postgresql://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgresql://cs2user:cs2password@localhost:5432/cs2_lineups?sslmode=disable

# Port du serveur (optionnel, défaut: 8080)
PORT=8080

# Environment (development, staging, production)
ENV=development

# Pour production Scalingo, DATABASE_URL est automatiquement définie par l'addon PostgreSQL
# Vous n'avez donc pas besoin de la configurer manuellement sur Scalingo
